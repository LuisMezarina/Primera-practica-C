#include <stdio.h>
#include <stdbool.h>

int main()
{
    int contador=0;

    for (contador=0; contador<10; contador++)
    {
        if (contador>0)
        {
            contador++;
        }
        printf("%d", contador);
        return 0; 
        
    }
    

    
    
    return 0;
}